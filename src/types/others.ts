export interface SubLesson {
    title: string;
    videoURL: string;
}

export interface CourseQueryResponse {
    name: string;
    price: number;
    rating: number;
    courseId: string;
    cover: string;
}